class MultiToddler {
    public values: number[];
    public orientation: string;

    constructor(values: number[], orientation: string) {
        this.values = values;
        this.orientation = orientation;
    }
}

export default MultiToddler